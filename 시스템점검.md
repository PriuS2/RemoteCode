# 시스템 점검 결과

> 최초 점검일: 2026-02-26
> 갱신일: 2026-02-27
> 대상: ClaudeCodeRemote (FastAPI + React)
> 진행률: **18 / 36** 수정 완료 (기존 30 + 신규 6)

---

## CRITICAL (즉시 수정 필요)

### 1. ~~CORS 설정 위험~~
- **파일:** `backend/main.py:91-99`, `backend/config.py:14`
- **내용:** `"*"` 설정 시 `allow_credentials=False`로 자동 강제 + WARNING 로그 출력. 특정 origin 설정 시에만 credentials 허용
- [x] 수정 완료 ✅ (2026-02-27 credentials 자동 비활성화)

### 2. ~~비밀번호 평문 비교~~
- **파일:** `backend/auth.py:19`
- **내용:** `hmac.compare_digest()`로 변경 완료. 기본값 `"changeme"` 하드코딩은 별도 이슈
- [x] 수정 완료 ✅ (2026-02-27 타이밍 공격 방지)

### 3. ~~하드코딩된 JWT 시크릿~~
- **파일:** `backend/config.py:3,11`, `backend/main.py:74-78`
- **내용:** 서버 시작(lifespan) 시 JWT 시크릿이 기본값이면 `RuntimeError`로 즉시 거부. `CCR_JWT_SECRET` 환경변수 설정 필수화
- [x] 수정 완료 ✅ (2026-02-27 시작 시 거부)

### 4. ~~python-jose 보안 취약점~~
- **파일:** `backend/requirements.txt:7`, `backend/auth.py:6,9`
- **내용:** `python-jose[cryptography]==3.3.0` → `PyJWT==2.10.1`로 교체. `JWTError` → `InvalidTokenError`로 변경. encode/decode API 호환
- [x] 수정 완료 ✅ (2026-02-27 PyJWT 교체)

---

## HIGH (조기 수정 권장)

### 5. 무제한 파일시스템 접근
- **파일:** `backend/main.py` (다수 엔드포인트)
- **내용:** `/api/browse`(140), `/api/files`(206), `/api/file-content`(310), `/api/file-raw`(335), `/api/upload`(382), `/api/mkdir`(356) 등 모든 파일 API에 경로 제한 없음. `os.path.abspath()` 사용하나 샌드박스 없음. 인증 토큰 하나 유출 시 전체 파일시스템 접근 가능
- **수정:** 허용 루트 디렉토리 설정 또는 세션의 work_path 기반 제한
- [ ] 수정 완료

### 6. ~~os.startfile() 임의 실행 위험~~
- **파일:** `backend/main.py:293`
- **내용:** `os.path.isdir()` 검증 추가. 디렉토리가 아닌 경로는 400 에러 반환
- [x] 수정 완료 ✅ (2026-02-27 디렉토리만 허용)

### 7. XSS 위험 - dangerouslySetInnerHTML
- **파일:** `frontend/src/components/FileExplorer.tsx:1773`
- **내용:** `highlight.js` 출력을 `dangerouslySetInnerHTML`로 렌더링. hljs가 내부적으로 HTML 이스케이프하나, 방어 계층 없음
- **수정:** DOMPurify 등 HTML 새니타이저 추가 또는 안전한 렌더링 방식 사용
- [ ] 수정 완료

### 8. ~~Error Boundary 없음~~
- **파일:** `frontend/src/components/ErrorBoundary.tsx`, `frontend/src/main.tsx`
- **내용:** `ErrorBoundary` 클래스 컴포넌트 생성, `main.tsx`에서 `<App />` 래핑. 에러 발생 시 메시지 표시 + Reload 버튼 제공
- [x] 수정 완료 ✅ (2026-02-27)

---

## MEDIUM (개선 권장)

### 9. ~~로그인 Rate Limiting 없음~~
- **파일:** `backend/main.py:13-14,42,110-116`
- **내용:** `slowapi`를 사용하여 IP당 5회/분 제한 구현됨. `CF-Connecting-IP` 헤더 지원으로 Cloudflare 환경 대응. 429 응답 핸들러 포함
- [x] 수정 완료 ✅ (2026-02-27 확인)

### 10. 동기 파일 I/O가 이벤트 루프 차단
- **파일:** `backend/main.py:167,237,320-323,344,373,404-414`
- **내용:** `os.scandir()`, `open()`, `os.path.getsize()`, `os.makedirs()` 등이 async 핸들러에서 동기 실행되어 전체 서버 응답 지연
- **수정:** `asyncio.to_thread()`로 감싸기
- [ ] 수정 완료

### 11. ~~SQL 인젝션 가능성 (컬럼 이름)~~
- **파일:** `backend/database.py:84-90`
- **내용:** `_ALLOWED_COLUMNS` 화이트리스트 추가. 허용되지 않은 컬럼명 시 `ValueError` 발생
- [x] 수정 완료 ✅ (2026-02-27)

### 12. ~~WebSocket 메시지 유효성 검사 없음~~
- **파일:** `backend/websocket.py:41-63`
- **내용:** JSON 파싱 에러 처리, `.get()` 안전 접근, 타입 검증(`isinstance`), resize 범위 제한(cols: 1-500, rows: 1-200) 추가
- [x] 수정 완료 ✅ (2026-02-27)

### 13. 전역 DB 연결 경합 조건
- **파일:** `backend/database.py:9-18`
- **내용:** 단일 전역 `aiosqlite.Connection` 공유. `get_db()`에서 두 코루틴이 동시에 `_db is None`을 볼 수 있는 race condition. 연결 복구 메커니즘 없음
- **수정:** 연결 풀 또는 `asyncio.Lock` 추가
- [ ] 수정 완료

### 14. ~~예외 상세 정보 클라이언트 노출~~
- **파일:** `backend/main.py` (6곳)
- **내용:** 모든 `str(e)` → 일반 에러 메시지로 변경, `logger.error()`로 서버 측 로깅 추가
- [x] 수정 완료 ✅ (2026-02-27)

### 15. Stale Closure 버그 - selectSession
- **파일:** `frontend/src/App.tsx:216-253`
- **내용:** `selectSession` 내부에서 `activeSessions.length`(236행)와 `mountedSessions.includes()`(248행)가 클로저로 이전 상태 참조. `setActiveSessions`는 콜백형으로 올바르나, 직후의 상태 읽기가 stale
- **수정:** `setActiveSessions` 업데이터 함수 내에서 체크하거나 ref 사용
- [ ] 수정 완료

### 16. ~~API 호출 에러 처리 없음~~
- **파일:** `frontend/src/App.tsx:301-380`
- **내용:** `handleSuspend`, `handleResume`, `handleTerminate`, `handleDelete`, `handleRename` 5개 함수에 `try/catch` + `console.error` 추가
- [x] 수정 완료 ✅ (2026-02-27)

### 17. mountedSessions 무제한 증가
- **파일:** `frontend/src/App.tsx:248-249`
- **내용:** 세션 선택 시 `mountedSessions`에 추가만 되고, suspend/terminate/delete 시에만 제거. 각 마운트 세션이 xterm Terminal + WebSocket + ResizeObserver 유지하여 메모리/CPU 낭비
- **수정:** 최대 마운트 수 제한 또는 LRU 방식으로 오래된 세션 언마운트
- [ ] 수정 완료

### 18. useEffect 의존성 누락 - FolderBrowser
- **파일:** `frontend/src/components/FolderBrowser.tsx:114-116`
- **내용:** `useEffect(() => { browse(initialPath || ""); }, [])` — `browse` 함수와 `initialPath` prop이 의존성 배열에 없음. `browse`도 `useCallback`으로 메모이제이션되지 않음
- **수정:** `browse`를 `useCallback`으로 감싸고 의존성 배열 수정
- [ ] 수정 완료

---

## LOW (시간될 때 개선)

### 19. ~~함수 내부 import문~~
- **파일:** `backend/main.py` (상단)
- **내용:** `string`, `datetime`, `subprocess`, `platform`을 파일 상단으로 이동 완료. `database.update_session`은 순환 import 방지를 위해 함수 내부 유지 (주석 표기)
- [x] 수정 완료 ✅ (2026-02-27)

### 20. /api/browse와 /api/files 코드 중복
- **파일:** `backend/main.py:140-188,206-280`
- **내용:** 경로 해석, 드라이브 열거(150-156 ↔ 216-222 동일 코드), 부모 계산, 권한 에러 처리 등 거의 동일한 로직 중복
- **수정:** 공통 로직 함수로 추출
- [ ] 수정 완료

### 21. ~~드라이브 목록 매 요청마다 열거~~
- **파일:** `backend/main.py:37-54`
- **내용:** `_get_drives()` 헬퍼 함수 추가. 모듈 레벨 캐시 + TTL 30초. `/api/browse`와 `/api/files` 양쪽에서 공유
- [x] 수정 완료 ✅ (2026-02-27)

### 22. ThreadPool 고갈 위험
- **파일:** `backend/pty_manager.py:13`
- **내용:** `max_workers=20` 고정. 설정 불가. 20개 이상 세션 동시 활성 시 전체 터미널 무응답
- **수정:** 환경변수로 설정 가능하게 하거나 세션 수 제한
- [ ] 수정 완료

### 23. ~~Session 인터페이스 중복 정의~~
- **파일:** `frontend/src/types/session.ts`
- **내용:** `types/session.ts`로 추출, `App.tsx`와 `SessionList.tsx`에서 import로 변경
- [x] 수정 완료 ✅ (2026-02-27)

### 24. FolderBrowser 경로 구분자 하드코딩
- **파일:** `frontend/src/components/FolderBrowser.tsx:396`
- **내용:** 백슬래시(`\\`)로 하드코딩. 드라이브 표시(319행)도 Windows 전용
- **수정:** 서버 응답의 구분자 사용 또는 양쪽 모두 지원
- [ ] 수정 완료

### 25. isMobile() 윈도우 리사이즈 미반영
- **파일:** `frontend/src/App.tsx:214`, `frontend/src/components/Terminal.tsx:77-78`
- **내용:** 일반 함수로 `window.innerWidth <= 768` 체크. 디바이스 회전이나 윈도우 리사이즈 시 자동 재렌더링 안됨
- **수정:** `useState` + resize 이벤트 리스너로 반응형 처리
- [ ] 수정 완료

### 26. ~~style 태그 컴포넌트마다 중복 삽입~~
- **파일:** `frontend/src/App.css` (상단)
- **내용:** `@keyframes ccr-spin`, `@keyframes ccr-pulse`를 `App.css`로 이동. `SessionList.tsx`의 인라인 `<style>` 태그 제거
- [x] 수정 완료 ✅ (2026-02-27)

### 27. 키보드 접근성 미지원
- **파일:** 다수 컴포넌트
- **내용:** `role="button"` 사용 0건. `FolderBrowser` 폴더 행, `SessionList` 컨텍스트 메뉴/세션 행, `App.tsx` 사이드바 백드롭 등 클릭 가능 `<div>`에 `tabIndex`, `onKeyDown` 없음
- **수정:** ARIA 속성 추가
- [ ] 수정 완료

### 28. ~~mkdir 이름 검증 불완전~~
- **파일:** `backend/main.py:364-377`
- **내용:** `..`, null 바이트, Windows 예약 이름(`CON`~`LPT9`), 특수문자(`<>:"\|?*`), 후행 점/공백 모두 차단하도록 종합 검증 추가
- [x] 수정 완료 ✅ (2026-02-27)

### 29. 업로드 파일 타입 제한 없음
- **파일:** `backend/main.py:382-423`
- **내용:** `os.path.basename()`으로 경로 탐색만 방지. 확장자/MIME 검증 없이 `.exe`, `.bat`, `.dll` 등 모든 파일 업로드 가능. 100MB 크기 제한만 존재. #6과 결합 시 원격 코드 실행 가능
- **수정:** 허용/차단 확장자 설정
- [ ] 수정 완료

### 30. ~~deprecated asyncio.get_event_loop()~~
- **파일:** `backend/pty_manager.py:129,149`
- **내용:** `asyncio.get_running_loop()`로 변경 완료
- [x] 수정 완료 ✅ (2026-02-27)

---

## 신규 발견 (2026-02-27)

### 31. localStorage 토큰 만료 없음
- **심각도:** MEDIUM
- **파일:** `frontend/src/App.tsx:24-26`
- **내용:** `localStorage.getItem("token")`으로 토큰 저장. 만료 시간 체크 없이 영구 지속. 토큰 유출(XSS 등) 시 무기한 유효
- **수정:** 토큰에 클라이언트 측 TTL 체크 추가 또는 `sessionStorage` 사용 고려
- [ ] 수정 완료

### 32. fetchSessions 에러 무시
- **심각도:** LOW
- **파일:** `frontend/src/App.tsx:126-128`
- **내용:** `catch { // ignore }` — 세션 폴링 네트워크 오류가 완전히 무시됨. 사용자에게 데이터 stale 상태 알림 없음
- **수정:** 연속 실패 시 사용자에게 연결 상태 표시
- [ ] 수정 완료

### 33. 드래그 이벤트 리스너 누수
- **심각도:** LOW
- **파일:** `frontend/src/App.tsx:80-104,148-168`
- **내용:** 사이드바/분할 드래그 시 `document`에 `mousemove`/`mouseup` 리스너 추가. `mouseup` 이벤트 누락(마우스가 윈도우 밖으로 나가거나 모달 방해) 시 리스너 잔존
- **수정:** `useEffect` cleanup 또는 `pointerCapture` 사용
- [ ] 수정 완료

### 34. XTerm useEffect 의존성으로 인한 터미널 재생성
- **심각도:** LOW
- **파일:** `frontend/src/components/Terminal.tsx:281`
- **내용:** xterm 초기화 `useEffect`가 `[sendInput, sendResize]`에 의존. WebSocket 재연결 시 콜백 참조 변경 → 전체 터미널 dispose 후 재생성 → 터미널 히스토리 소실
- **수정:** 콜백을 ref로 래핑하여 의존성에서 제거
- [ ] 수정 완료

### 35. React.StrictMode 미적용
- **심각도:** LOW
- **파일:** `frontend/src/main.tsx`
- **내용:** `<React.StrictMode>` 없이 `<App />` 직접 렌더링. 개발 모드에서 unsafe lifecycle, 부수효과 중복 실행 등을 감지 불가
- **수정:** `<React.StrictMode>` 래핑 추가
- [ ] 수정 완료

### 36. ~~root 요소 Non-null assertion~~
- **심각도:** LOW
- **파일:** `frontend/src/main.tsx`
- **내용:** null 체크 후 명확한 에러 메시지 throw하도록 변경
- [x] 수정 완료 ✅ (2026-02-27)
